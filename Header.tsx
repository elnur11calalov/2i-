import { LogOut, Users, Sun, Moon } from 'lucide-react'
import { useState } from 'react'
import { useAuth } from '../../contexts/AuthContext'
import type { UserProfile } from '../../types'
export default function Header({ profile, page, setPage }: { profile: UserProfile; page: 'home' | 'friends'; setPage: (page: 'home' | 'friends') => void }) { const { logout } = useAuth(); const [dark, setDark] = useState(document.documentElement.classList.contains('dark')); const toggleDark = () => { document.documentElement.classList.toggle('dark'); setDark(!dark) }; return <header className="mb-6 flex items-center justify-between gap-3"><button onClick={() => setPage('home')} className="flex min-w-0 items-center gap-3 text-left"><img src={profile.photoURL ?? `https://ui-avatars.com/api/?name=${encodeURIComponent(profile.displayName)}`} className="h-10 w-10 rounded-full" alt="Profil" /><span className="min-w-0"><strong className="block truncate">{profile.displayName}</strong><small className="block truncate text-slate-500">ID: {profile.uid.slice(0, 8)}</small></span></button><div className="flex items-center gap-1"><button aria-label="Rəng rejimi" onClick={toggleDark} className="btn p-2 text-slate-600 dark:text-slate-300">{dark ? <Sun size={19} /> : <Moon size={19} />}</button><button aria-label="Dostlar" onClick={() => setPage('friends')} className={`btn p-2 ${page === 'friends' ? 'bg-green-100 text-green-700' : 'text-slate-600 dark:text-slate-300'}`}><Users size={20} /></button><button aria-label="Çıxış" onClick={() => logout()} className="btn p-2 text-slate-600 dark:text-slate-300"><LogOut size={20} /></button></div></header> }
