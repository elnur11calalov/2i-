import { Check, Circle } from 'lucide-react'
export default function TaskCard({ label, done, onClick }: { label: string; done: boolean; onClick: () => void }) { return <button onClick={onClick} className={`flex w-full items-center justify-between rounded-xl border p-4 text-left ${done ? 'border-green-200 bg-green-50 dark:border-green-900 dark:bg-green-950/40' : 'border-slate-200 bg-slate-50 dark:border-slate-700 dark:bg-slate-800'}`}><span className="font-medium">{label}</span><span className={`inline-flex items-center gap-1.5 rounded-lg px-3 py-1.5 text-sm font-semibold ${done ? 'bg-green-600 text-white' : 'bg-slate-200 text-slate-600 dark:bg-slate-700 dark:text-slate-300'}`}>{done ? <Check size={16} /> : <Circle size={16} />}{done ? 'Qeyd edildi' : 'Qeyd edilməyib'}</span></button> }
